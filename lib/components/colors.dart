import 'package:flutter/material.dart';

const primaryColor = Color(0xff1649c6);
const secondaryColor = Color(0xfffb9323);
const accentColor = Color(0xffffc428);
const unselectedColor = Color(0xffacacac);
const primaryGradient = LinearGradient(
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    colors: [
      primaryColor,
      Colors.blue,
    ]);
